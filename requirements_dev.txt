# DatasetSanity Development Requirements (Python 3.8 compatible)

# Build tooling first (helps avoid PEP517/build-isolation surprises)
pip>=23.1
setuptools==65.5.0
wheel==0.37.1
build==0.10.0
twine==4.0.2

# Docs
ensure==1.0.2
mkdocs-material==9.5.3

# Testing
pytest==6.2.5
pytest-cov==3.0.0
tox==3.25.1
coverage==6.5.0

# Linting & formatting
flake8==5.0.4
black==22.12.0
isort==5.10.1
pre-commit==2.20.0

# Type checking
mypy==0.991
types-requests==2.31.0

# Jupyter support (Python 3.8 compatible)
jupyterlab==3.6.7

# Windows-only: pin to a wheel-available version to avoid source builds
pywinpty==2.0.10; platform_system=="Windows"

# Editable install of your package
-e .

# Integration test deps
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=1.0.0